version: "3"
services:
  app:
    container_name: app
    restart: always
    build:
     context: ./
     dockerfile: app.dockerfile
    ports:
      - "3000:3000"
    environment:
      - "DB_HOST=database"
    depends_on:
      - mongo
  mongo:
    container_name: mongo
    image: mongo
    volumes:
      - mongodata:/data/db
    environment:
      - "MONGO_INITDB_ROOT_USERNAME: esteban"
      - "MONGO_INITDB_ROOT_PASSWORD: 123456"
    ports:
      - "27017:27017"
volumes:
  mongodata:
